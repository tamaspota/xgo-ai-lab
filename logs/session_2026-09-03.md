# Session 2026-09-03

## Objective

Initialize the repository and recover/program an original Kickstarter-era XGO-Mini for later child-friendly robotics and AI experiments.

## Work completed

- Created repository operating instructions and handoff documentation.
- Cloned repository locally to `C:\projects\xgo-ai-lab` and created a Python virtual environment.
- Installed Silicon Labs CP210x VCP driver.
- XGO USB-UART enumerates as **COM3**, VID:PID `10C4:EA60`, serial `0001`, location `1-2`.
- Reviewed current `LuwuDynamics/xgo_doglib`; documented constructor reset behavior.
- Added raw XGO firmware-read probe and tested it on COM3; **no response**.
- Verified from period documentation and RobotShop legacy product data that the original generation is **K210 + STM32** with 240x240 LCD, OV2640 camera, 16 GB SD, MEMS mic and three programmable keys.
- Reviewed original XGO-Mini Communication Protocol V1.0: STM32 motion control uses a separate XH2.54 4-pin TTL UART at 115200 8N1, normally occupied by the AI module.
- Corrected working model: COM3 is likely the K210 USB/programming/console path, not a transparent STM32 protocol bridge.
- Tamás explicitly approved replacement of old software/firmware when the target image is hardware-compatible.
- Added `scripts/k210_serial_probe.py`.

## K210 serial tests

Passive test:

```powershell
python scripts\k210_serial_probe.py COM3 --listen 5
```

Observed:

```text
passive RX bytes: 6
hex: 0d 0a 0d 0a 0d 0a
```

This is three CR/LF pairs, proving at least some serial activity on COM3.

REPL test:

```powershell
python scripts\k210_serial_probe.py COM3 --listen 2 --repl
```

Observed:

- no passive bytes in the initial 2 s window;
- no bytes after Ctrl-C;
- no `>>>` prompt.

Interpretation: no interactive MaixPy/MicroPython REPL is exposed in this state. This does **not** prove failure because historical XGO K210 software runs a custom menu/application.

## RobotShop research

RobotShop still hosts the discontinued legacy XGO-Mini page and period K210 manual.

Confirmed legacy specifications:

- K210 + STM32;
- 240 x 240 LCD;
- OV2640 0.3 MP camera;
- 16 GB SD;
- MEMS microphone;
- three programmable keys;
- Blockly and Python programming via PC/app;
- open serial protocol for secondary development.

RobotShop product page:

- https://eu.robotshop.com/products/xgo-mini-quadruped-robot-dog

Period manual:

- https://cdn.robotshop.com/media/x/xgo/rb-xgo-05/pdf/xgo-mini_k210_4-1-.pdf

## Historical K210 software/firmware source discovered

Historical parent repository:

- https://github.com/geluu/XgoAI

Useful fork:

- https://github.com/mynameiskristopher/k210-XgoAI

The English branch contains:

- `xgo-ai-module-firmware-210722-en-2021-07-22-14-48-10.kfpkg`;
- `sd/main.py`;
- `sd/xgo.py`;
- `sd/device_test.py`;
- `sd/try_demo.py`;
- language/preset/user directories;
- Blockly XML examples.

Historical `sd/xgo.py` uses MaixPy `machine.UART` at 115200 and implements the original XGO motion command set. Historical `sd/main.py` runs a custom LCD/menu application and loads user/demo code from SD.

Important limitation: this repository is a strong period recovery lead but is **not yet verified as an official Luwu/XGO vendor repository**. The dated `.kfpkg` is therefore a recovery candidate, not an automatically trusted flash image.

Established K210 flash utility if later needed:

- https://github.com/sipeed/kflash_gui

## Current interpretation

The current evidence is better explained by a working K210 USB-UART path with custom application firmware than by a dead robot controller. The next lowest-risk check is boot-time capture plus SD inspection, not immediate firmware flashing.

## Next concrete actions

1. Start a longer listener and power-cycle the robot while it is running:

```powershell
cd C:\projects\xgo-ai-lab
git pull
.\.venv\Scripts\Activate.ps1
python scripts\k210_serial_probe.py COM3 --listen 20
```

2. Photograph the powered LCD/menu.
3. Remove/read the microSD card if practical and back up its complete contents before changing K210 flash.
4. Compare installed SD contents with the historical `XgoAI` tree.

If SD recovery does not restore useful behavior, evaluate the dated 2021 K210 `.kfpkg` with `kflash_gui` after verifying the board/recovery path.

## Safety / compatibility guard

- Do not flash current ESP32 XGO-Mini lower-board firmware onto the original STM32 board.
- Do not overwrite STM32 until an original-generation-compatible image is identified.
- K210 rewrite is allowed after recovery compatibility is checked.

## Uncommitted / unfinished work

No known GitHub-side unfinished change after this update. Hardware bring-up remains active.
